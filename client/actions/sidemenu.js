import {
  CLOSE_SIDE_MENU,
  OPEN_SIDE_MENU
} from '../constants/actionTypes'

export const closeSideMenu = () => ({
  type: CLOSE_SIDE_MENU
})

export const openSideMenu = () => ({
  type: OPEN_SIDE_MENU
})